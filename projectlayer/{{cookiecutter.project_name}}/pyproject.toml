[project]
name = "{{ cookiecutter.project_name }}"
version = "0.1.0"
description = "Generated Python project."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "debugpy"]

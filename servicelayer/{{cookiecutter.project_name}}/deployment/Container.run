ARG REGISTRY=docker.io
FROM ${REGISTRY}/python:3.12-slim 
WORKDIR /env
COPY --from=build_{{cookiecutter.project_build_id | replace('"','')}} /env /env
ENV VIRTUAL_ENV=/env/.venv
ENV PATH="$VIRTUAL_ENV/bin:$PATH"
ENV PYTHONPATH=/env
CMD ["uv", "run", "python", "app/main.py"]
